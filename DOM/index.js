const { default: axios } = require("axios");

// 1. Axios to GET questions from backend
axios.get()

// 2. Build a form in the DOM using the questions

// 3. When the user submits the form, and Axios to POST the answers to the backend to add to the database